import { Command } from '@react-native-community/cli-types';

const runWasmCommand: Command = {
  name: 'run-wasm',
  description: 'builds your app and starts it',
  func: async () => {},
  options: [],
};

const linkWasmCommand: Command = {
  name: 'autolink',
  description: 'autoliking',
  func: async () => {},
  options: [],
};
